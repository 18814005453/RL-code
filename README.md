# my-rl-journey

从零到博士级 RL 研究能力 — **只放已完成的项目成品代码，不推草稿。**

## 已完成

| # | 项目 | 描述 | 核心产出 |
|---|------|------|---------|
| L1 | micrograd | 手写 autograd 引擎 + MLP + 四步训练循环 | `engine.py` `nn.py` `test.py` |
| L2 | makemore-bigram | 纯统计概率模型（数数→概率→采样），中英文对比 | `bigram.py` |

## 运行

```bash
cd L1-micrograd && python3 test.py
```

## 规则

- ✅ 放完成的项目代码（可一键运行）
- ❌ 不放草稿、临时文件、别人的 repo、空文件夹
- 每完成一个项目，加一个文件夹
